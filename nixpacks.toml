[phases.setup]
nixPkgs = ["nodejs_20", "openssl"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["npm run db:generate", "npm run build"]

[start]
cmd = "./node_modules/.bin/next start --hostname 0.0.0.0 --port $PORT"
